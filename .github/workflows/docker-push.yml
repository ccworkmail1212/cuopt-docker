name: Build and Push Docker Images

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  push-src:
    name: Push cuopt-src (source code image)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Clone ccworkmail1212/cuopt source
        run: git clone --depth=1 https://github.com/ccworkmail1212/cuopt.git /tmp/cuopt-src

      - name: Copy Dockerfile.src into source dir
        run: cp Dockerfile.src /tmp/cuopt-src/

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: workcc
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push cuopt-src
        uses: docker/build-push-action@v5
        with:
          context: /tmp/cuopt-src
          file: /tmp/cuopt-src/Dockerfile.src
          push: true
          tags: workcc/cuopt-src:26.6

  push-dev:
    name: Build and push cuopt-dev (dev container)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Clone ccworkmail1212/cuopt source
        run: git clone --depth=1 https://github.com/ccworkmail1212/cuopt.git /tmp/cuopt-src

      - name: Copy Dockerfile.dev-official into source dir
        run: cp Dockerfile.dev-official /tmp/cuopt-src/

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: workcc
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push cuopt-dev
        uses: docker/build-push-action@v5
        with:
          context: /tmp/cuopt-src
          file: /tmp/cuopt-src/Dockerfile.dev-official
          push: true
          tags: workcc/cuopt-dev:26.6.0a
          cache-from: type=gha
          cache-to: type=gha,mode=max

  push-package:
    name: Tag cuopt-custom:latest
    runs-on: ubuntu-latest
    steps:
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: workcc
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Pull and tag as latest
        run: |
          docker pull workcc/cuopt-custom:26.6.0a
          docker tag workcc/cuopt-custom:26.6.0a workcc/cuopt-custom:latest
          docker push workcc/cuopt-custom:latest
          echo "cuopt-custom:latest updated"
